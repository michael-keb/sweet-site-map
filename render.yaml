services:
  - type: web
    name: squad-finance-website
    runtime: static
    repo: https://github.com/michael-keb/sweet-site-map
    branch: main
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
